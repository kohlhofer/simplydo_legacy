<li id="<%= "todo#{todo.id}" %>">
  <%= todo.title %>
  <% if todo.done == 0 %>
    <%= check_box('done', todo.id, :class => "tickBox", :onclick => remote_function( :url => { :action => "close", :id => todo.id })) %>
  <% else %>
    <%= check_box('done', todo.id, :class => "tickBox", :checked => 'checked', :onclick => remote_function( :url => { :action => "reopen", :id => todo.id })) %>
  <% end %>
</li>